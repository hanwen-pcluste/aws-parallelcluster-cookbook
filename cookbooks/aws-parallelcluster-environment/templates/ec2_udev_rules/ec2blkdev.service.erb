[Unit]
Description=Setup ec2blkdev service
After=<%= @network_target %>

[Service]
ExecStart=/sbin/udevadm trigger --action=change --subsystem-match=block; sleep 15

[Install]
WantedBy=multi-user.target
